import createTheme from '@mui/material/styles/createTheme';

export const theme = createTheme({
  palette: {
    background: {
      default: '#fefbf0',
    },
    primary: {
      main: '#631f28',
    },
  },
});
